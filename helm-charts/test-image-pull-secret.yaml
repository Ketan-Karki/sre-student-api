# Test file with image pull secrets
nameOverride: ""
fullnameOverride: ""

# Image credentials for private registry access
imageCredentials:
  registry: "docker.io"
  username: "your-username"
  password: "your-password"
  email: "your-email@example.com"
  createSecret: true
  secretName: "registry-credentials"

# Namespace configuration
namespace:
  create: true
  name: "student-api-test"

# Rest of the values remain the same as default
